import { LitElement } from "lit-element";


/**
 * An element that allows the user to select and upload files.
 */
export class Uploader extends LitElement {
  static tagName: string = "uploader";

  /**
   * @inheritDoc
   */
  protected render() {

  }
}
